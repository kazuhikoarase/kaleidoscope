
	//calculate the height property for the inner equi edge tri-angle 
	//in the square.
	// TODO: since change the value of len, can adjust the shape of unit
	// try to use a normal tri-angle, a square, and a circle as the shape.
	//  equi-edge triangle:
	//  { len : rect * Math.sqrt(3) / 2, ox : rect / 2, oy : rect / 4}
	//  largest normal tri-angle : 
	//  {len : rect, ox : (rect + rect + 0 ) / 3, oy : (0 + 0 + rect) / 3}
    var len = rect ;
    var ox = len * 2 / 3;
    var oy = rect / 3;
    var angle = 0;

((rect * Math.sqrt(3) / 2 )/ Math.sqrt(3) )/ 2;
